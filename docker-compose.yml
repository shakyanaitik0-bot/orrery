services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: orrery
      POSTGRES_PASSWORD: orrery
      POSTGRES_DB: orrery
    ports:
      - "5432:5432"
    volumes:
      - orrery-db:/var/lib/postgresql/data

volumes:
  orrery-db:
